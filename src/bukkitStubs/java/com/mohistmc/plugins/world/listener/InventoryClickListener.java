package com.mohistmc.plugins.world.listener;

import org.bukkit.event.inventory.InventoryClickEvent;

/**
 * Compile-time stub only (never packaged into the mod jar). Its sole purpose is to let the
 * Mixin annotation processor resolve the target method while compiling
 * {@code MohistInventoryGuardMixin}. At runtime the real Mohist class is used.
 */
public class InventoryClickListener {

    public static void init(InventoryClickEvent event) {
    }
}
